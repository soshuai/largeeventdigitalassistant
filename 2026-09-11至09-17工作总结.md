# 手持端改动说明（2026-09-11 ～ 2026-09-17）

本文按工作包整理，便于对客户报价与验收，不按内部提交号罗列。覆盖范围：**9 月 11 日「新机器」起至 9 月 17 日车证核验上传/查询对齐为止**。

---

## 1. 新机型 / 硬件适配（9 月 11 日）

- 适配新手持设备：中控身份证 / RFID、串口 so、相关 jar。
- 接入身份证读取、扫码辅助能力。
- 活动设置按 **人证 / 车证分模块**（位置、分区、设备注册相互独立）。
- 新增 **证件查询** 页面（查询结果展示场馆 / 分区 / 区域权限）。

---

## 2. 手柄触发核验（9 月 15 日）

- 手柄按键可触发人证核验（主流程在首页）。
- 车证侧同步接入手柄触发，避免只能点屏幕。

---

## 3. 权限语义与接口整理（9 月 15 日）

- 纠正场馆 / 分区 / 区域与后台字段对应关系（`venue`、`venuePartition`、`venueArea`）。
- 统一、补全接口 DTO 与接口层结构，设置页、人证/车证核验、详情展示一起对齐。

---

## 4. 活动设置：权限数据流重做（9 月 16～17 日）

### 数据来源

- `getBasicInfo` **只用于位置、分区下拉**，不再用里面的权限字典铺列表，也不再作为核验回退。
- 选齐位置 + 分区 → **注册设备**。
- 注册成功后拉权限并展示、写入核验缓存：
  - 人证：`getMatrixAuthInfoList`
  - 车证：新增 `getCarMatrixAuthList`（入参、返参与人证同类）

### 注册策略

- 同一活动、同一模块、同一位置 + 分区：**不重复注册**，但仍会 **重新请求权限**（防止后台改过权限）。
- 换位置或分区：先注册，成功后再拉权限。
- **注册失败**：清空该模块权限列表和本地缓存，避免沿用上一组权限。

### 列表展示（人证 / 车证不同）

| 模块 | 展示列 | 字段 |
| --- | --- | --- |
| 人证 | 场馆、分区、区域 | `venue` + `venueVal`；`venuePartition` + `venuePartitionVal`；`venueArea` + `venueAreaVal` |
| 车证 | 场馆、停车通行码 | `venue` + `venueVal`；`park` + `parkVal`（隐藏分区、区域） |

- 分区展示 **只用 `venuePartitionVal`**，不用整条记录的 `name`。
- 过滤后台占位值：`NONE` / `NULL` 等不展示，核验视为该维未配置。

---

## 5. 人证核验规则调整

- 只比 **场馆、分区、区域**（对应证件 `venuePrivileges` / `zonePrivileges` / `areaPrivileges`）。
- **分项**（`sportProject` / `sportPrivileges`）设置页没有，核验已停用（代码注释保留，便于以后打开）。
- 放宽规则：
  - 三列都空 → 提示未配置场馆/分区/区域，不通过。
  - 设备某维空 → 不比该维。
  - 证件某维空 → 跳过该维（不当成无权限）。
  - 两边都有值 → 必须匹配；`ALL` / `INF` 该维不限制。
- 无权时结果页和日志会标明失败维度。
- 人证权限失败日志补全（此前日志写在未调用的方法里，核验路径看不到原因）。

---

## 6. 车证核验与详情（字段与比对）

核验改为：

- 设备：`venue`、`park`
- 证件：`venueCodeChildren`、`parkingCode`

规则与人证同类：都空要提示；有值才比；证件空则跳过；两维都有则都要过。有场馆、停车空则 **只比场馆**。

详情展示改为：

- 单位：`applicantOffice`
- 车牌：`licensePlateNum`
- 编号：`cardId`
- 权限：场馆 + 停车通行码（不再用旧 `area` / `parkingArea` 当区域）

其他：

- 未激活状态判断调整（`eventStatus` 为 4 或 5 视为已激活）。

---

## 7. 车证核验上传、记录查询对齐人证（9 月 17 日）

此前核验比对和详情已切新字段，上传和历史查询仍是旧模型。本期已按人证接口结构替换。

### 上传：`POST androidNew/api/receiveCheckCar`

请求体字段与人证 `receiveCheckPerson` 一致，单条 JSON 对象，不再使用 `{ "data": "[...]" }` 包装。

主要字段：`entryId`、`verifyStatus`、`verifyTime`、`direction`（0 进 / 1 离）、`equipmentId` / `equipmentName`、`locationId` / `locationName`、`zoneLocationId` / `zoneLocationName`、`name`、`cardNumber`、`cardId`、`createTime`、防疫三项、`activityId`、`imageName`、`remark`。

车证侧取值约定：

- 位置 / 分区：读 **车证模块** 已选位置、分区
- `name`：车牌
- `cardNumber`：芯片号
- `cardId`：车证编号
- `verifyStatus`：与人证同一套结果码
- 无现场照时 `remark` 写核验说明

### 查询：`POST androidNew/api/selectCarCertCheckRecord`

入参、返参与人证 `selectPersonCheckRecord` 一致。

- **入参**：`current`、`size`、`name`、`cardNumber`、`cardId`、`activityId`、`verifyStatus`（null 全部 / 1 成功 / 0 失败）、`equipmentName`、`locationName`、`beginTime`、`endTime`
- **返参单条**：`entryId`、`verifyStatus`、`verifyTime`、`direction`、设备 / 位置 / 分区、`name`、`cardNumber`、`cardId` 等
- 记录列表：车牌走 `name`，芯片号走 `cardNumber`，状态码与人证同一套展示

---

## 工作量

1. 新机型硬件 + 读证 / 串口 + 证件查询
2. 手柄触发
3. 权限字段纠偏与接口整理
4. 活动设置权限链路（注册、人证/车证分接口、刷新 / 失败清空、列表字段）
5. 人证三维核验放宽 + 分项停用 + 占位过滤
6. 车证场馆 + 停车通行码核验与详情字段
7. 车证核验记录上传 + 记录查询 / 列表展示与人证接口对齐

---

## 验收时可对照的接口

| 能力 | 路径 | 说明 |
| --- | --- | --- |
| 人证权限列表 | `GET androidNew/api/getMatrixAuthInfoList` | 设置页展示 + 核验缓存 |
| 车证权限列表 | `GET androidNew/api/getCarMatrixAuthList` | 入参返参与人证同类 |
| 人证核验上传 | `POST androidNew/api/receiveCheckPerson` | 单条对象 |
| 车证核验上传 | `POST androidNew/api/receiveCheckCar` | 字段与人证上传一致 |
| 人证记录查询 | `POST androidNew/api/selectPersonCheckRecord` | 分页 |
| 车证记录查询 | `POST androidNew/api/selectCarCertCheckRecord` | 字段与人证查询一致 |
